<%= form_for(@preregistration) do |f| %>
  <% if @preregistration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@preregistration.errors.count, "error") %> prohibited this preregistration from being saved:</h2>

      <ul>
      <% @preregistration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
