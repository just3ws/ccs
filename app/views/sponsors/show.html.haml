%h4.section-title sponsor description 
= render :partial => "sponsor", :locals => {:sponsor => @sponsor}

- if user_signed_in? && (current_user.admin? || current_user.speaker?)
  = render :partial => "shared/actions", :locals => {:model => @sponsor, :current_action => :show}

