%h4 Upload a CSV file to import into the database
- form_for @import, :html => {:multipart => true} do |f|
  = f.file_field :csv
  %select{:name => "import[datatype]", :size => "1"}
    %option{:value => "speakers"} Speakers
  = f.submit "Import"
