.show
  %h3= link_to "#{schedule.time_slot.start_and_end_time}", schedule.time_slot
  %ul
    %li.schedule.session
      %span session:
      = link_to schedule.sesja.title, sesja_path(schedule.sesja)

